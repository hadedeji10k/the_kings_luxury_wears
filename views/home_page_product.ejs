<%- include ('_layouts/header1') %> 


    <!--========== single product ==========-->
    <section class="about section bd-container">
        <div class="about__container  bd-grid">
            <img src="/home_images/<%= p.id %>/<%= p.image %>" alt="" class="about__img">
            <div class="about__data">
                <h2 class="section-subtitle about__initial"><%= title %> </h2>
                <!-- <h4 class="section-title about__initial"></h4> -->
                <h4>Product Details:</h4>
                <p class="about__description"><%= p.desc %> </p> <br>
                <h3>&#8358;<%= parseFloat(p.price).toFixed(2) %> </h3>
                <del>&#8358;<%= parseFloat(p.discountPrice).toFixed(2) %> </del> <br> <br>
                <a href="/cart/add/home-page/<%= p.slug %>" class="button">Add to cart</a>
            </div>
        </div>
        <br><br>
        <div class="bd-container">
            <ul class="gallery">
                <% galleryImages.forEach(image => { %>
                    <% if (image != "thumbs") { %>
                        <li>
                            <a data-fancybox="gallery" href="/home_images/<%= p.id %>/gallery/<%= image %>">
                                <img src="/home_images/<%= p.id %>/gallery/thumbs/<%= image %>" >
                            </a>
                        </li>
                    <% } %>
                <% }) %>
            </ul>
        </div>
    </section>

<br><br>
<%- include ('_layouts/footer1') %> 